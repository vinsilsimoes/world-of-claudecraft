// Shared collection-operation limits. UI projections consume the same values
// as the authoritative commands so controls never advertise rejected work.

export const MIR4_COLLECTION_COMBINE_ALL_LIMIT = 25_000;
